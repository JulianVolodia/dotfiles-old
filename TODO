deploy:
=======

* Docstring
* Logging
* Remove stale symlinks

Zsh:
====

* Figure out how to incorporate virtualenv into path
* venv: magenta if not in venv (ex.:)
* Show applied patches

Gpg-agent:
==========

* See if I can omit some values in the config file or on the
  command-line

Vim:
====

* Use dmenu for files/buffers
* Find better keys for Ctrl, Esc, CapsLock
* Use one vim server per virtualenv and per LaTeX project (localvimrc)

Mutt-kz:
========

* Trash patch
* Show mailboxes in sidebar hierarchically
* Add some useful keybindings & get familiar with them
* How does it work?
* Is spam filtering by pressing button ("mark as spam" or somesuch) possible?
  * macro index,pager S "<save-message>=[Gmail]/Spam?<enter>" "mark message as spam
  * macro index,pager H "<save-message>=INBOX<enter>" "mark message as ham
* IMAPfilter
* Reorganize config files
* Gmail hardlinks
