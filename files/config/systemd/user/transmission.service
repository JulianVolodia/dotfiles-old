[Unit]
Description=Transmission Daemon
Wants=network.target
# Only start this automatically when not on battery
ConditionACPower=true

[Service]
ExecStart=/usr/bin/transmission-daemon -f
Restart=on-abort

[Install]
WantedBy=common.target
