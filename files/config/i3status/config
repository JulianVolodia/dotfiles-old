general {
    colors = true
    color_good = "#f9f8f5"
    color_degraded = "#fd971f"
    color_bad = "#f92672"
    color_separator = "#fd971f"
    interval = 5
}

order += "battery 0"
/* order += "volume master" */
order += "time"

battery 0 {
    format = "⚡ %percentage %remaining"
}

# Other unicode characters:
# ⌚ ⧖ ⧗
time {
    format = "⧖ %H:%M"
}
